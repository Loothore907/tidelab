# TL-002: synthetic H1 conservative execution and trial accounting

Date: 2026-09-25. Owner: [issue #48](https://github.com/Loothore907/tidelab/issues/48). This check uses invented prices, liquidity, accounts, and product rules. Its figures are test expectations, not market performance.

The H1 v1 historical replay and local paper probe now call the same TideLab percentage price and fee arithmetic: base adverse price adjustment 0.10% and fee 0.25%, with the preregistered doubled stress case. The local execution decision additionally requires a UTC observation within two minutes, eight-decimal quantity, displayed synthetic size, a minimum order value, and a price within the proposal's limit. Missing, stale, zero-size, and over-limit observations hold without a fill. These paper constraints do not make hourly OHLCV an executable quote or establish a real fill probability. The historical full-fill replay remains an explicit scenario assumption.

Against pinned LEAN `88bce0fc6fe282378ee73c54cef1090d0d7a73ee`, a frozen C# H1 entry proposal generated one mock-broker submission. An invented 99.8 opening reference with 10 units available produced a 10-unit partial buy at 99.8998, fee 2.497495, cash 8998.504505, and 15 units unfilled. The test source canceled the remainder. The exact terminal report/account hash, with no interim new submission, prepared the next frozen-policy 10-unit exit. An invented 98.2 reference produced a full sell at 98.1018, fee 2.452545, final cash 9977.069960 and zero holding. An independent decimal calculation checked both report executions, cash transitions and 4.950040 total fees. Fresh LEAN setups after partial, cancel and final fill sent no new orders. The final intent remains `submission_unknown` because no subsequent opening-account handoff was requested.

The deterministic H1 replay now exposes trial metrics for strategy, cash, 25% buy-and-hold and fully invested buy-and-hold under base and stress costs: net return, maximum marked drawdown, total and longest underwater time in scored hourly closes, average exposure, turnover, fees, closed round trips, largest round-trip result and net result excluding that trade. It reconstructs cash/inventory from fills before reporting. The invented 171-bar fixture yields one strategy round trip and verifies deterministic replay and cost stress; its numbers do not evaluate H1 on market data. The replay constructs those bars from `synthetic_input.json`, which the local experiment identity binds as its data hash. The cost hash binds replay, execution and metric source bytes. `record_local_trial.py` refuses a reused attempt ID before launching a command. An actual development attempt can be recorded only against the integrated source revision; validation and untouched periods remain unopened.

Focused command: `TL_H1_COST_ONLY=1 bash scripts/lean_fallback/run_forward_recovery_probe.sh <separate-pinned-LEAN-checkout> <dotnet-10-binary>`. The 2026-09-25 focused run built with 0 errors and 136 upstream dependency/toolchain warnings, then passed the costed partial/cancel/exit/restart sequence. The standalone H1 check passed its synthetic policy and trial-metric assertions. No account, real market data, provider feed, spend, trade, or public real-data artifact was used.

This leaves several #48 gates: a private historical runner must bind exact approved OKX input/provenance and the trial identity to the metrics; the source's current research/retention terms and hashes must be rechecked before any real-data run; and full-fill, fee, spread, slippage and missed-fill sensitivity remain assumptions rather than venue evidence. Issue #3 separately owns timely forward data. No profit, paper-readiness, or external-broker finality claim follows from this synthetic case.
